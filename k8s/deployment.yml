apiVersion: apps/v1
kind: Deployment
metadata:
  name: kubernetes_demo-api
  labels:
    app: kubernetes_demo-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: kubernetes_demo-api
  template:
    metadata:
      labels:
        app: kubernetes_demo-api
    spec:
      containers:
        - name: kubernetes_demo-api
          image: fad16papa/kubernetes_demo-api:latest
          ports:
            - containerPort: 3000
          env:
            - name: NODE_ENV
              value: "production"
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          readinessProbe:
            httpGet:
              path: /readyz
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: 3000
            initialDelaySeconds: 15
            periodSeconds: 20